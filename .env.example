# Sample environment configuration

# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=test1
POSTGRES_PORT=5432
DATABASE_URL=postgresql://postgres:postgres@db:5432/test1
POSTGRES_HOST_PORT=5433

# Bind service names
DB_HOST=db

# FastAPI
# Backend port (used by Docker and the app)
BACKEND_PORT=8000

# Front-end (optional)
# Frontend port (used by Docker and dev server)
FRONTEND_PORT=3000
