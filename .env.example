# Sample environment configuration

# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=test1
POSTGRES_PORT=5432
DATABASE_URL=postgresql://postgres:postgres@db:5432/test1
POSTGRES_HOST_PORT=5433

# Bind service names
DB_HOST=db

# FastAPI
# Backend port
BACKEND_PORT=8000

# Comma-separated list of allowed CORS origins
CORS_ORIGINS=http://localhost:3000,https://client-mt.netlify.app
INTERNAL_ADMIN_ORIGINS=http://admin.internal
# Optional regex override for internal admin HTTP origin matching
INTERNAL_ADMIN_ORIGIN_REGEX=^http://(localhost|127\\.0\\.0\\.1|10\\.\\d{1,3}\\.\\d{1,3}\\.\\d{1,3}|192\\.168\\.\\d{1,3}\\.\\d{1,3}|172\\.(1[6-9]|2\\d|3[0-1])\\.\\d{1,3}\\.\\d{1,3})(:\\d{2,5})?$

# Front-end (optional)
FRONTEND_PORT=3000
APP_PUBLIC_URL=http://localhost:3000

# Reverse proxy hosts (Caddy)
PUBLIC_API_HOST=api.38-79-154-248.nip.io
ADMIN_HOST=admin.38-79-154-248.nip.io

# LiqPay
LIQPAY_PUBLIC_KEY=sandbox
LIQPAY_PRIVATE_KEY=sandbox
LIQPAY_CURRENCY=UAH

# Admin credentials for /auth/login
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
# Token returned on successful login
ADMIN_TOKEN=adminsecret

# Secret key used to sign tokens
JWT_SECRET=changeme
TICKET_LINK_SECRET=changeme-ticket-link
TICKET_LINK_TTL_DAYS=3

# SMTP settings
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=example
SMTP_PASSWORD=changeme
SMTP_FROM=noreply@example.com
SMTP_FROM_NAME=Bus Tickets
